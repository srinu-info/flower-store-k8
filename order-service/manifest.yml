apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: store
  labels:
    project: online-store
    component: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      project: online-store
      component: order-service
  template:
    metadata:
      labels:
        project: online-store
        component: order-service
    spec:
      containers:
      - name: order-service
        image: devopsrinu/order-service:v1
        imagePullPolicy: Always
        ports:
          - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: store
  labels:
    project: online-store
    component: order-service
spec:
  selector:
    project: online-store
    component: order-service
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
